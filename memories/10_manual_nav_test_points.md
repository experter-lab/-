# Manual Nav Test Points

- 2026-06-20 confirmed-drive point: `map -> base_link` approximately `x=1.366`, `y=-0.030`, `yaw=7.46 deg`. The user confirmed the robot physically moved from the preceding Nav2 forward test. Use this as a known-good debug reference point for short forward/reverse validation, not as the Cartographer fixed-start pose.
- 2026-06-20 reverse short test from the confirmed-drive point: after setting `velocity_smoother.min_velocity[0]` to `-0.08`, a `linear.x=-0.08` pulse through `/cmd_vel_nav` reached `RC3=1585` and `SERVO7/8=(1396,1396)`, with TF estimating about `0.137 m` reverse motion. Final safe state was `BRAKE_STATUS_SAFE`. The post-reverse Cartographer startup pose was saved on the RK3588 as `x=1.214`, `y=-0.052`, `yaw=7.78 deg`.
- 2026-06-20 reverse repeat stability test: same `linear.x=-0.08` for about `1.4 s` again reached `RC3=1585` and `SERVO7/8=(1396,1396)`, so the ArduPilot output magnitude was repeatable. TF estimated about `0.057 m` reverse motion this time, smaller than the prior `0.137 m`; treat reverse physical distance as less stable than the electrical output until visually confirmed. The current Cartographer startup pose was saved as `x=1.122`, `y=-0.057`, `yaw=8.30 deg`.
- Correction from the user on 2026-06-20: during both reverse tests, the robot did not physically move; only the motor made sound. The TF changes to `x=1.214` and `x=1.122` were localization/odom drift under motor vibration or scan changes, not real reverse motion. Do not treat lidar-only TF movement as physical motion unless the user visually confirms chassis movement. The saved startup pose was restored to the pre-reverse confirmed-drive point `x=1.366`, `y=-0.030`, `yaw=7.46 deg`, and `carto_apply_saved_pose.py` was run so current TF returned to about `x=1.367`, `y=-0.033`, `yaw=7.44 deg`.
- 2026-06-20 reverse threshold electrical test: temporarily set `velocity_smoother.min_velocity[0]` to `-0.12` and tested `/cmd_vel_nav` reverse pulses `-0.08`, `-0.10`, `-0.12` for about `1.0 s` each. Confirmed `MANUAL base=193` and final `BRAKE_STATUS_SAFE`. Electrical output scaled consistently: `-0.08 -> RC3=1585, SERVO7/8=1396`; `-0.10 -> RC3=1602, SERVO7/8=1352`; `-0.12 -> RC3=1613, SERVO7/8=1332`. Runtime reverse limit was restored to `-0.08` afterward. Physical reverse movement remains unconfirmed; rely on user visual feedback, not TF drift.
- 2026-06-20 after reverse-threshold tests, the user manually moved the robot back to a visually scan-map fitted pose in RViz. Saved this fitted pose to `/mnt/sdcard/medicine_robot_data/config/carto_initial_pose.json`: `x=1.128`, `y=-0.048`, `yaw=-1.32 deg`. For future sessions, use this as the current trusted startup pose. Lesson: short reverse pulses can make lidar-only TF/base_link unreliable; after reverse testing, trust visual scan-map fit or user-confirmed physical position before saving poses or sending Nav2 goals.
- 2026-06-20 stable motion window found after manual refit: repeated direct reverse tests with `/cmd_vel_nav linear.x=-0.10` for `2.5 s` were physically smooth and scan stayed fitted; smoother limited actual reverse target to `-0.08`, with repeatable output `RC3=1585`, `SERVO7/8=(1396,1396)`. A direct forward test with `/cmd_vel_nav linear.x=+0.10` for `2.0 s` was also physically smooth and scan stayed fitted; output reached `RC3=1425`, `SERVO7/8=(1619,1619)`.
- 2026-06-20 small closed-loop test: saved trusted start pose `x=0.845`, `y=-0.014`, `yaw=0.09 deg`, then ran a Nav2 `NavigateToPose` forward goal of about `0.18 m`. Nav2 succeeded (`status=4`) and TF estimated `0.101 m` forward. Then direct reverse `/cmd_vel_nav linear.x=-0.10` for `1.5 s` returned near the start, with final TF delta from start about `0.016 m`. Final check was `BRAKE_STATUS_SAFE`. Do not over-trust sub-5 cm TF errors; user visual scan fit remains the ground truth.
- 2026-06-20 Nav2 25 cm forward test: `NavigateToPose` goal about `0.25 m` ahead succeeded (`status=4`), TF estimated about `0.20 m`, and the user confirmed RViz scan stayed fitted. Saved the new trusted fitted startup pose on the RK3588 as `x=1.077`, `y=0.002`, `yaw=-1.08 deg`. Final state was `BRAKE_STATUS_SAFE`.
- 2026-06-20 fixed-point navigation phase: replaced the board `stations.yaml` example coordinates with real map test stations around the current fitted pose: `pharmacy/current=(1.382,-0.017,-0.045rad)`, `test_forward_20=(1.582,-0.027,-0.045rad)`, `test_back_30=(1.082,-0.004,-0.045rad)`, plus `ward_a/ward_b/ward_c` aliases. Restarted `rk3588_delivery_webctl.sh`; `medicine_task_manager` reported stations `nurse_station, pharmacy, test_back_30, test_forward_20, ward_a, ward_b, ward_c` with `navigation_backend=nav2`.
- 2026-06-20 global planning probe for the new stations: `/compute_path_to_pose` succeeded without motion for `pharmacy`, `test_forward_20`, `test_back_30`, `ward_a`, `ward_b`, and `ward_c`. Initial path lengths were about `0.006m`, `0.200m`, `0.275m`, `0.200m`, `0.275m`, and `0.561m`.
- 2026-06-20 first task-manager fixed-point Nav2 test: created a real `/medicine/create_delivery_task` task to `test_forward_20` with load already confirmed, so `medicine_task_manager` sent `/navigate_to_pose`. Nav2 reached `WAITING_DISPENSE_CONFIRMATION`, then the task was canceled and robust safe stop restored `BRAKE_STATUS_SAFE`. Start TF `x=1.370,y=-0.006,yaw=-2.54deg`; end TF `x=1.474,y=-0.013,yaw=-2.99deg`; TF delta about `0.104m`. Treat this as proof that the station -> task manager -> Nav2 -> controller -> chassis chain works, but not as a precision arrival result: the 20cm target is short relative to the current `xy_goal_tolerance=0.08m`, and Nav2 accepted arrival while feedback had recently reported about `0.12m` remaining.
- 2026-06-20 after first fixed-point test: current TF around `x=1.476,y=-0.008,yaw=-2.93deg`; plan-only checks still succeeded to `pharmacy` (`0.142m` path), `test_back_30/ward_b` (`0.317m`), `test_forward_20` (`0.110m`), and `ward_c` (`0.360m`). For short reverse station tests, DWB needs negative `FollowPath.min_vel_x`; local config was changed from `0.02` to `-0.08` to match the already-tested `velocity_smoother.min_velocity[0] = -0.08`.
- 2026-06-20 deployed reverse-capable local planning: synced `rk3588_nav2_params.yaml` to the board with `FollowPath.min_vel_x=-0.08`, restarted Nav2 only, and confirmed runtime `/controller_server FollowPath.min_vel_x` is `-0.08` while `/velocity_smoother min_velocity` remains `[-0.08, 0.0, -0.05]`. Added board scripts `/mnt/sdcard/rk3588_station_plan_probe.sh` for plan-only station checks and `/mnt/sdcard/rk3588_fixed_point_nav_task_test.sh` for task-manager fixed-point Nav2 tests with robust safe-stop trap.
- 2026-06-20 fixed-point reverse local planning test: after clearing one stale `ros2 service call /chassis_bridge/...` client left by a timeout, ran `/mnt/sdcard/rk3588_fixed_point_nav_task_test.sh test_back_30 45`. The first attempt aborted before navigation because `/chassis_bridge/set_mode` timed out during enable; robust stop restored `BRAKE_STATUS_SAFE`. The second attempt succeeded: task manager accepted task `ecaa26b5`, Nav2 output negative local velocity (`cmd_nav=(-0.080,-0.100)`, smoothed `/cmd_vel=(-0.080,-0.050)`), TF moved from about `x=1.472,y=-0.009,yaw=-2.7deg` to `x=1.187,y=-0.003,yaw=-0.32deg`, and the state reached `WAITING_DISPENSE_CONFIRMATION`. The task was canceled and final safety check was `BRAKE_STATUS_SAFE`.
- 2026-06-20 after reverse fixed-point test: current TF was about `x=1.200,y=-0.001,yaw=-0.57deg`; plan-only checks still succeeded to `pharmacy` (`0.249m`), `test_forward_20` (`0.535m`), `test_back_30` (`0.167m`), and `ward_c` (`0.690m`). This is the current best baseline for station navigation: station file is real, global planning works, task-manager-to-Nav2 works, forward local planning works, and reverse local planning works after enabling negative DWB `min_vel_x`.
- 2026-06-21 medium forward fixed-point test to `ward_c`: before motion, plan-only from current `x=1.197,y=0.001,yaw=-0.52deg` to `ward_c=(1.781,-0.036)` succeeded with about `0.631m` path. The real task-manager Nav2 run timed out instead of reaching the goal; it moved only to about `x=1.409,y=0.014,yaw=-0.89deg`, then was manually robust-stopped and final state was `BRAKE_STATUS_SAFE`/task `IDLE`. Logs show `bt_navigator` began from `(1.20,0.00)` to `(1.78,-0.04)`, but feedback distance stayed around `0.49m`, then `0.44m`, sometimes `0.35m`, before cancellation. Post-stop plan-only still succeeded to `ward_c` (`0.574m`) and other stations, so this was not a global-planner failure; treat it as a local execution/clearance/traction or low-speed progress issue. Do not keep forcing longer forward goals until RViz/real clearance and command/PWM traces are checked.
- 2026-06-21 arc-turn test: pure Nav2 /spin on this chassis was ineffective; it only produced steering noise and no meaningful body rotation. A backward arc test using linear.x=-0.08, ngular.z=+0.10 for 2.5 s was successful: steering_pwm moved from 1500 to 1413, 	hrottle_pwm moved from 1514 to 1585, and TF estimated about -26.35 deg heading change with about  .114 m travel. Final state returned to BRAKE_STATUS_SAFE.
- 2026-06-21 after successful relocalization to trajectory 5, ran /mnt/sdcard/rk3588_relative_back_nav_test.sh 0.10 75. Plan succeeded with 5 poses and length 0.103 m. Nav2 result status=4, start TF x=0.189 y=0.021 yaw=-3.02 deg, goal x=0.089 y=0.026, end TF x=0.074 y=0.084 yaw=-4.03 deg, TF delta distance=0.131 m, dx=-0.115, dy=0.063, dyaw=-1.01 deg. Final state was BRAKE_STATUS_SAFE. User visual scan-fit confirmation still needed for physical validity.
- Correction from user on 2026-06-21: the 10 cm Nav2 backward test after trajectory 5 did not physically move the robot. Treat its reported TF delta distance=0.131 m as localization/scan-match drift, not chassis movement. Do not count this as a successful closed-loop motion. Next reverse tests must log live /cmd_vel and chassis PWM/servo output and require user visual confirmation.
